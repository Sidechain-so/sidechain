import React from "react";
import withLayout from "../../components/layout/withLayout";

const HirePage = () => {
  return (
    <div>
      <h2>HirePage</h2>
    </div>
  );
};

export default withLayout(HirePage);
